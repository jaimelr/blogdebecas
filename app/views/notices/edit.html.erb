<div class="content">
  <h2>Actualizar beca</h2>
  <section class="section--basic">
    <%= form_with model: @notice, method: :patch, class: 'form' do |form| %>
      <% render partial: 'form', locals: { form: form, submit_text: 'Actualizar aviso' } %>
    <% end %>
  </section>
</div>
