<div class="scholarship scholarship-new">
  <h2>Crear beca</h2>
    <section class="section--basic">
      <%= form_with model: @scholarship, class: 'form' do |form| %>
      <%= render partial: 'form_fields', locals: { form: form, submit_text: 'Crear beca' } %>
    <% end %>
  </section>
</div>
